<% if(typeof effective != 'undefined' && effective){
  if(effective.high || effective.low) { %>
    "effectivePeriod" : {
      "high": "<%- effective.high %>",
      "low" :  "<%- effective.low %>"
    }
  <% }else { %>
    "effectiveDateTime" : "<%- effective %>"
 <% }
} %>