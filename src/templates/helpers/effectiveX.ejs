<% if(typeof effective != 'undefined' && effective){ %>
  <%- (typeof excludePreComma !== 'undefined' && excludePreComma) ? '' : ',' %>
  <% if(effective.high || effective.low) { %>
    "effectivePeriod" : {
      "high": "<%- effective.high %>",
      "low" :  "<%- effective.low %>"
    }
  <% }else { %>
    "effectiveDateTime" : "<%- effective %>"
 <% }
} %>